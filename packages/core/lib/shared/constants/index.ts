const rosepackConfigFile = "rosepack.ts";

const rosepackCacheName = "rosepack";

const rosepackEnvFile = "rosepack.d.ts";

const rosepackTmpConfigFile = "config.mjs";

const packageJsonFile = "package.json";

const tsConfigFile = "tsconfig.json";

export {
  rosepackConfigFile,
  rosepackEnvFile,
  rosepackCacheName,
  rosepackTmpConfigFile,
  packageJsonFile,
  tsConfigFile,
};